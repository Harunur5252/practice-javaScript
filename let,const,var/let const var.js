/*
let  : use let only when needed to re-assign or change a value
const : most of the time use const for declare a variable but do not use re-assign.Any program start with const if 
        then needed to change value then use let.
var : do not use var.

Example : let firstName = 'harun'
          firstName='nazim'
          console.log(firstName) // will show nazim that means changeable

          const lastName = 'roshid'
                lastName = 'rakib'
                console.log(lastName) // will show error
                
*/
